{
  "name": "dead-code",
  "version": "1.0.0",
  "description": "",
  "type": "module",
  "author": "",
  "scripts": {
    "serve": "serve",
    "build": "esbuild index.js --bundle --format=esm --outfile=dist.js --minify --sourcemap"
  },
  "license": "ISC",
  "dependencies": {
    "bulma": "^0.9.2",
    "date-fns": "^2.21.3"
  },
  "devDependencies": {
    "esbuild": "^0.12.0",
    "prettier": "^2.3.0",
    "serve": "^11.3.2"
  }
}
