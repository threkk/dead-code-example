{
  "name": "dead-code",
  "version": "1.0.0",
  "description": "Date comparison tool used as an example for a dead code detection algorithm.",
  "type": "module",
  "author": "Alberto de Murga <alberto@threkk.com>",
  "scripts": {
    "serve": "serve",
    "build": "esbuild src/index.js --bundle --format=esm --outfile=docs/dist.js --minify --sourcemap",
    "format": "prettier"
  },
  "license": "MIT",
  "dependencies": {
    "date-fns": "^2.21.3",
    "dayjs": "^1.10.6"
  },
  "devDependencies": {
    "esbuild": "^0.12.0",
    "prettier": "^2.3.0",
    "serve": "^11.3.2"
  }
}
